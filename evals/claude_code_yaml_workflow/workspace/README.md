# Claude Code YAML stress workspace

Greenfield mini-project used only for **`evals/claude_code_yaml_workflow/run_stress.py`**.

The agent’s shell **working directory** for stress runs is this folder (not the repository root). Read **`AGENTS.md`** for the full build specification.
